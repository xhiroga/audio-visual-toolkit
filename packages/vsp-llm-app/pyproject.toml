[project]
name = "vsp-llm-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "vsp-llm",
]

[tool.uv.sources]
vsp-llm = { path = "../../../VSP-LLM" } # TOBEFIX
